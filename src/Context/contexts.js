import { createContext } from "react"

/**
 * Contains design details for the html page
 */
export const pageDesignContext = createContext();

/**
 * Contains user details
 */
export const userDetailsContext = createContext();

